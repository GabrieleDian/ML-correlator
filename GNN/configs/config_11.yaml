# Configuration file for GNN experiments

# Data configuration
data:
  base_dir: '../Graph_Edge_Data'  # Path to data directory
  train_loop_order: [7,8,9,10]             # Which loop to use for training
  test_loop_order: 11                  # Which loop to use for testing
  
# Feature configuration
features:
  # Features to compute (when running compute_features.py)
  compute_all: true
  features_to_compute:
    - eigen_1
    - eigen_2
    - eigen_3
    - degree
    - betweenness
    - clustering
    - pagerank
    - closeness
    - face_count

  # Features to use for training
  selected_features:
  - eigen_1
  - eigen_2
  - eigen_3
  - degree
  - betweenness
  - clustering
  - pagerank
  - closeness
  - face_count

    
  # Computation settings
  chunk_size: 1000
  n_jobs: 16

# Model configuration
model:
  name: "gin"              # Model architecture: gin, gat, hybrid, planar, simple
  hidden_channels: 64
  num_layers: 3
  dropout: 0.2

# Training configuration
training:
  epochs: 100
  batch_size: 256
  learning_rate:   0.0005026963339910785
  weight_decay:   2.7148197332317223e-05
  scheduler_type: "onecycle"  # Options: onecycle, plateau, none
  
# Experiment configuration
experiment:
  seed: 42
  use_wandb: true
  wandb_project: "train_7_8_9_10_test_11_loop_all_scalar"
  wandb_name: "gin_simple"
  save_model: false
  model_dir: "models"