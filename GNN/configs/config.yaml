# Configuration file for GNN experiments

# Data configuration
data:
  base_dir: '../Graph_Edge_Data'  # Path to data directory
<<<<<<< Updated upstream
  train_loop_order: ['7','8','9','10']           # Which loop to use for training
  test_loop_order: '11'   
  extra_train: true              # Which loop to use for testing
  
# Feature configuration
features:
  # Features to compute (when running compute_features.py)
  compute_all: true
  features_to_compute:
    - identity_columns
    - adjacency_columns
    - eigen_1
    - eigen_2
    - eigen_3
    - degree
    - betweenness
    - pagerank
    - closeness
    - clustering
    - face_count

=======
  train_loop_order: ['6','7','8','8to10']           # Which loop to use for training
  test_loop_order: '9'   
# Feature configuration
features:
>>>>>>> Stashed changes
  # Features to use for training
  selected_features:
   - eigen_1
   - eigen_2
   - eigen_3
   - degree
   - betweenness
   - pagerank
   - closeness
   - clustering
   - face_count
    
  # Computation settings
  chunk_size: 1000
  n_jobs: 4

# Model configuration
model:
  name: "gin"              # Model architecture: gin, gat, hybrid, planar, simple
  hidden_channels: 512
  num_layers: 3
  dropout: 0.4

# Training configuration
training:
  epochs: 100
  batch_size: 256
  learning_rate:   0.00028721941495727597
  weight_decay:   0.00003060739988229477
  scheduler_type: "onecycle"  # Options: onecycle, plateau, none
  threshold: 0.5              # Decision threshold for classification
  log_threshold_curves: false   # Whether to log threshold sweep curves to WandB
  
# Experiment configuration
experiment:
  seed: 42
  use_wandb: true
<<<<<<< Updated upstream
  wandb_project: "train_7_8_9_10_test_11_all_scalar"
  wandb_name: "gin_simple"
  model_dir: "models"
  save_model: true
=======
  wandb_project: "train_6_7_8_8to10_test_9_all_scalar"
  wandb_name: "gin_simple"
  model_dir: "models"
  save_model: true
>>>>>>> Stashed changes
