# Configuration file for GNN experiments

# Data configuration
data:
  base_dir: '../Graph_Edge_Data'  # Path to data directory
  loop_order: 8                   # Which loop to use
  
# Feature configuration
features:
  # Features to compute (when running compute_features.py)
  compute_all: true
  features_to_compute:
    - identity_columns
    - adjacency_columns
    - eigen_1
    - eigen_2
    - eigen_3
    - degree
    - betweenness
    - clustering
    - closeness
    - pagerank
    - face_count
  
  # Features to use for training
  selected_features:
    - 
    - adjacency_columns
    - eigen_1
    - eigen_2
    - eigen_3
    - degree
    - betweenness
    - clustering
    - closeness
    - pagerank
    - face_count
  # Computation settings
  chunk_size: 1000
  n_jobs: 4

# Model configuration
model:
  name: "gin"              # Model architecture: gin, gat, hybrid, planar, simple
  hidden_channels: 32
  num_layers: 2
  dropout: 0.32723750287593667

# Training configuration
training:
  epochs: 100
  batch_size: 32
  learning_rate: 0.005945469231707924
  weight_decay: 0.000402816483040196
  scheduler_type: "onecycle"  # Options: onecycle, plateau, none
  
# Experiment configuration
experiment:
  seed: 42
  use_wandb: true
  wandb_project: "all_features_8_loop_opt"
  wandb_name: "gin_simple"
  save_model: true
  model_dir: "models"
